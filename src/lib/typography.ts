/** Matches root layout `--font-cabinet` (Cabinet Grotesk via next/font). */
export const cabinetHeading = "font-[family-name:var(--font-cabinet)] font-extrabold";

/** Wordmarks and titles that should use Cabinet but not extrabold weight. */
export const cabinetBold = "font-[family-name:var(--font-cabinet)] font-bold";
